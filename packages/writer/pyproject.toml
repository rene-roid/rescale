[project]
name = "rescale-writer"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mutagen>=1.47",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["rescale_writer"]
